name: typescript test

on: 
  workflow_dispatch:

jobs:
  ts-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - working-directory: ts-test/
        run: |
          npm ci
          ts-node test.ts